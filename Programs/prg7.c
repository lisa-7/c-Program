//porgram to get square root of given number
#include<stdio.h>
#include<math.h>

void main()
{
    int n=16;
    double v;
    //formula for square root
    v = sqrt(n);
    //%f is used for float that is 0.00
    printf("Square Root of %d is %f", n, v);
}